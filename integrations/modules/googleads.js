// Google Ads Conversion Tracking
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for googleads.
module.exports={id:'googleads',name:'Google Ads Conversion Tracking',configure:(values={})=>({id:'googleads',configured:Object.keys(values).length>0})};
