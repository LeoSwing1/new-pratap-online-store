// Meta Ads & Conversion Tracking
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for meta.
module.exports={id:'meta',name:'Meta Ads & Conversion Tracking',configure:(values={})=>({id:'meta',configured:Object.keys(values).length>0})};
