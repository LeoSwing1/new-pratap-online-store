// Google Merchant / Shopping
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for merchant.
module.exports={id:'merchant',name:'Google Merchant / Shopping',configure:(values={})=>({id:'merchant',configured:Object.keys(values).length>0})};
