// Supplier & Purchase Management
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for supplier.
module.exports={id:'supplier',name:'Supplier & Purchase Management',configure:(values={})=>({id:'supplier',configured:Object.keys(values).length>0})};
