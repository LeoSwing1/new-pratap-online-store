// Live Human Support
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for live-support.
module.exports={id:'live-support',name:'Live Human Support',configure:(values={})=>({id:'live-support',configured:Object.keys(values).length>0})};
