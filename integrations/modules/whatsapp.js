// WhatsApp Business
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for whatsapp.
module.exports={id:'whatsapp',name:'WhatsApp Business',configure:(values={})=>({id:'whatsapp',configured:Object.keys(values).length>0})};
