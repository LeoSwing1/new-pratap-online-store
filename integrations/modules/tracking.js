// AWB & Live Tracking
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for tracking.
module.exports={id:'tracking',name:'AWB & Live Tracking',configure:(values={})=>({id:'tracking',configured:Object.keys(values).length>0})};
