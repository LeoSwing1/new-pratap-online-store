// Google Maps & Address Intelligence
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for maps.
module.exports={id:'maps',name:'Google Maps & Address Intelligence',configure:(values={})=>({id:'maps',configured:Object.keys(values).length>0})};
