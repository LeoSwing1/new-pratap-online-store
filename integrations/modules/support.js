// Support & Smart Assistant
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for support.
module.exports={id:'support',name:'Support & Smart Assistant',configure:(values={})=>({id:'support',configured:Object.keys(values).length>0})};
