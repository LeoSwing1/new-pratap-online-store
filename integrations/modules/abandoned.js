// Abandoned Cart Recovery
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for abandoned.
module.exports={id:'abandoned',name:'Abandoned Cart Recovery',configure:(values={})=>({id:'abandoned',configured:Object.keys(values).length>0})};
