// WhatsApp Product Catalogue
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for catalog.
module.exports={id:'catalog',name:'WhatsApp Product Catalogue',configure:(values={})=>({id:'catalog',configured:Object.keys(values).length>0})};
