// Advanced Search & Smart Filters
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for search.
module.exports={id:'search',name:'Advanced Search & Smart Filters',configure:(values={})=>({id:'search',configured:Object.keys(values).length>0})};
