// Reviews & Ratings
// Provider credentials are configured server-side through the Integration Control Center.
// This module is the stable contract entry point for reviews.
module.exports={id:'reviews',name:'Reviews & Ratings',configure:(values={})=>({id:'reviews',configured:Object.keys(values).length>0})};
